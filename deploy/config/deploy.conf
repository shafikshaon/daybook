# Deployment Configuration
# Edit these values before deployment

# Server Details
SERVER_IP="13.228.221.238"

# Application
APP_NAME="daybook"
APP_USER="daybook"
APP_GROUP="daybook"

# Directories
APP_DIR="/home/ubuntu/projects/opt/daybook"
BACKEND_DIR="${APP_DIR}/backend"
FRONTEND_DIR="${APP_DIR}/frontend"
LOG_DIR="/var/log/daybook"

# Database
DB_NAME="daybook_prod"
DB_USER="daybook_user"
DB_PASSWORD="" # Will be generated if empty

# Domain/IP (for Nginx)
DOMAIN_OR_IP="daybook.shafik.xyz"

# Ports
BACKEND_PORT=8080
FRONTEND_PORT=3000

# Go Version
GO_VERSION="1.25.3"

# Node Version
NODE_VERSION="20"
